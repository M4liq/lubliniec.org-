<!DOCTYPE html>

<title>ArticlePhoto</title>

{% block body %}
    <h1>ArticlePhoto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article_photo.id }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ article_photo.path }}</td>
            </tr>
            <tr>
                <th>Section_no</th>
                <td>{{ article_photo.sectionNo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('article_photo_index') }}">back to list</a>

    <a href="{{ path('article_photo_edit', {'id': article_photo.id}) }}">edit</a>

    {{ include('article_photo/_delete_form.html.twig') }}
{% endblock %}

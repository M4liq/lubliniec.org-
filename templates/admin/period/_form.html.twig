<div class="col-12">
    <div class="col-4"> 
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-success">{{ button_label|default('Zapisz') }}</button>
    {{ form_end(form) }}
    </div>
</div>

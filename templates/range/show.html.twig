{% extends 'base.html.twig' %}

{% block title %}Range{% endblock %}

{% block body %}
    <h1>Range</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ range.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ range.name }}</td>
            </tr>
            <tr>
                <th>Lower</th>
                <td>{{ range.lower }}</td>
            </tr>
            <tr>
                <th>Upper</th>
                <td>{{ range.upper }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('range_index') }}">back to list</a>

    <a href="{{ path('range_edit', {'id': range.id}) }}">edit</a>

    {{ include('range/_delete_form.html.twig') }}
{% endblock %}

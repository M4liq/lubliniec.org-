<form method="post" action="{{ path('article_photo_delete', {'id': article_photo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article_photo.id) }}">
    <button class="btn">Delete</button>
</form>
